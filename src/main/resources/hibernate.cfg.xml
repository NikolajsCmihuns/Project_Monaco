<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <property name="connection.url">jdbc:mysql://localhost:3306/monaco</property>
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.username">root</property>
        <property name="connection.password">Maxims</property>
        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Enable Hibernate's automatic session context management -->
        <property name="current_session_context_class">thread</property>

        <!-- Disable the second-level cache -->
        <property name="cache.provider_class">org.hibernate.CacheMode</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>
        <!-- DAO class maping area -->
        <mapping class="janis.monaco.domain.CityRef"/>
        <mapping class="janis.monaco.domain.CommentOnReview"/>
        <mapping class="janis.monaco.domain.CountryRef"/>
        <mapping class="janis.monaco.domain.Place"/>
        <mapping class="janis.monaco.domain.PlacesInRoute"/>
        <mapping class="janis.monaco.domain.PlaceTypeRef"/>
        <mapping class="janis.monaco.domain.Review"/>
        <mapping class="janis.monaco.domain.Route"/>
        <mapping class="janis.monaco.domain.TagsRef"/>
        <mapping class="janis.monaco.domain.User"/>

    </session-factory>
</hibernate-configuration>